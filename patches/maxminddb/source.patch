# HG changeset patch
# Parent  f025f7bcf9522eceb3d811e95f61a0c63bac592a
Fixed 32/64-bit compiler warnings

diff -r f025f7bcf952 src/maxminddb.c
--- a/src/maxminddb.c	Wed Dec 05 23:26:54 2018 -0500
+++ b/src/maxminddb.c	Wed Dec 05 23:31:37 2018 -0500
@@ -434,7 +434,7 @@ LOCAL const uint8_t *find_metadata(const
                           METADATA_MARKER, marker_len);
 
         if (NULL != tmp) {
-            max_size -= tmp - search_area;
+            max_size -= (ssize_t) (tmp - search_area);
             search_area = tmp;
 
             /* Continue searching just after the marker we just read, in case
